# Work Done

## Setup

- [x] Initialized project
- [x] Clone go2rtc repository
- [x] Configure go2rtc for RTSP streaming from file (Docker)
- [x] Create web page for WebRTC playback

## RTSP Link

`rtsp://localhost:8554/stream1`

## WebRTC Player

You can use the built-in player or the custom `index.html` provided.
To use `index.html`, simply open it in your browser.
Ensure the `go2rtc` docker container is running:

```bash
docker-compose up -d
```
